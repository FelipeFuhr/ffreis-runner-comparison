name: Semgrep (SAST)

on:
  workflow_dispatch:
  schedule:
    - cron: "17 17 24 * *"  # monthly scattered (was: 0 8 * * 1)

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  semgrep:
    permissions:
      contents: read
      actions: read
      security-events: write
    uses: FelipeFuhr/ffreis-workflows-python/.github/workflows/python-semgrep.yml@359eb22d0d22b6266b4543764f1c8bebe5e84765 # v1.3.0
